name: 🧑‍🚀 Deploy projects

on:
  push:
    branches:
      - releases/*

jobs:
  build:
    uses: ./.github/workflows/actions/build.yml

  deploy:
    uses: ./.github/workflows/actions/deployment.yml
    needs: [build]
