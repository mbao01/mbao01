name: 🫳 Release packages

on:
  push:
    branches:
      - main
      - setup-release-scripts

jobs:
  build:
    uses: ./.github/workflows/actions/build

  deploy:
    uses: ./.github/workflows/actions/deployment
    needs: [build]
  
  release:
    uses: ./.github/workflows/actions/release
    needs: [build, deploy]
